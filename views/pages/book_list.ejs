<h1>
  <%= title %>
</h1>
<ul>
  <% book_list.forEach(book=> { %>
    <li class="list_item">
      <a href="<%= book.url %>">
        <%= book.title %>
      </a>
      <p>(<%= book.author.name %>)</p>
    </li>
    <% }) %>
</ul>